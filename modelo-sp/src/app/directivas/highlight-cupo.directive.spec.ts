import { HighlightCupoDirective } from './highlight-cupo.directive';

describe('HighlightCupoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCupoDirective();
    expect(directive).toBeTruthy();
  });
});
