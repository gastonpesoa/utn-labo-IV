import { HayCupoDirective } from './hay-cupo.directive';

describe('HayCupoDirective', () => {
  it('should create an instance', () => {
    const directive = new HayCupoDirective();
    expect(directive).toBeTruthy();
  });
});
