import { UserFilterTypePipe } from './user-filter-type.pipe';

describe('UserFilterTypePipe', () => {
  it('create an instance', () => {
    const pipe = new UserFilterTypePipe();
    expect(pipe).toBeTruthy();
  });
});
