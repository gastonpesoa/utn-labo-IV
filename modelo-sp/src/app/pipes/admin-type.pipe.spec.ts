import { AdminTypePipe } from './admin-type.pipe';

describe('AdminTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminTypePipe();
    expect(pipe).toBeTruthy();
  });
});
